[core]
	engine = pg
	plan_file = sqitch.plan
	top_dir = .
# [engine "pg"]
	# target = db:pg:
	# registry = sqitch
	# client = psql
[deploy]
	verify = true
[deploy "variables"]
	schema = app_public, app_hidden, app_private, app_runtime, public
